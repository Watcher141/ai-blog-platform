// Dither component commented out — requires @react-three/fiber, postprocessing, three
// which were removed for a lean install. Uncomment and reinstall those deps when needed.
export default function Dither() {
  return null;
}
